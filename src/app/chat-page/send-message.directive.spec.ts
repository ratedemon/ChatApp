import { SendMessageDirective } from './send-message.directive';

describe('SendMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new SendMessageDirective();
    expect(directive).toBeTruthy();
  });
});
